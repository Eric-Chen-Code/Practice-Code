// This module just provides the readstr() function from
// the notes (Section 10)

// readstr() reads in a new string from I/O or returns NULL if EOF
// effects: allocates memory (caller must free)
char *readstr(void);
