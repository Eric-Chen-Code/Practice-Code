#include <cstddef>
#include <string.h>
#include <iostream>
#include <map>

using namespace std;

double change = 1.00; //YO I'm not sure how u get the input but just put it here
int dollars = 0;
int quarters = 0;
int dimes = 0;
int nickels = 0;
int pennies = 0;

if (change == 0)
{
    return "No change";
}
else
{
    while (change != 0)
    {
    }
}